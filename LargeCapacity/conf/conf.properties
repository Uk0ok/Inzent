QUERY1=SELECT ELEMENTID, DESCR, USERSCLASS, ECLASSID, EXPIRATION FROM ( SELECT SEQ, ELEMENTID, DESCR, USERSCLASS, ECLASSID, EXPIRATION FROM	( SELECT ROWNUM AS SEQ, ELEMENTID, DESCR, USERSCLASS, ECLASSID, EXPIRATION FROM	( SELECT *	FROM ASYSELEMENT a	WHERE ELEMENTID LIKE '2%' ORDER BY ELEMENTID DESC )) WHERE SEQ >= ? ) WHERE ROWNUM <= 10000
QUERY2=SELECT ELEMENTID, VOLUMEID, FILEKEY, FILESIZE, CREATEDATE, LASTACCESS, CONTENTTYPE, CONTENTCLASSID, GENERALDATA, CHECKHINT, WRITEDATE, RETENTION, CHECKSUM FROM ( SELECT SEQ, ELEMENTID, VOLUMEID, FILEKEY, FILESIZE, CREATEDATE, LASTACCESS, CONTENTTYPE, CONTENTCLASSID, GENERALDATA, CHECKHINT, WRITEDATE, RETENTION, CHECKSUM FROM ( SELECT ROWNUM AS SEQ, ELEMENTID, VOLUMEID, FILEKEY, FILESIZE, CREATEDATE, LASTACCESS, CONTENTTYPE, CONTENTCLASSID, GENERALDATA, CHECKHINT, WRITEDATE, RETENTION, CHECKSUM FROM ( SELECT * FROM ASYSCONTENTELEMENT a ORDER BY ELEMENTID DESC )) WHERE SEQ >= ? ) WHERE ROWNUM <= 10000
QUERY3=INSERT INTO ASYSELEMENT VALUES (?, ?, ?, ?, ?)
QUERY4=INSERT INTO ASYSCONTENTELEMENT VALUES (?, ?, ?,  CAST(? as unsigned), STR_TO_DATE(?, '%Y-%m-%d %H:%i:%s'), STR_TO_DATE(?, '%Y-%m-%d %H:%i:%s'), CAST(? as unsigned), ?, ?, CAST(? as unsigned), STR_TO_DATE(?, '%Y-%m-%d %H:%i:%s'), CAST(? as unsigned), ?)

FILEPATH=C:\\dev\\sam\\err
DELIMETER=|
